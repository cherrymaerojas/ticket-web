{
    "devDependencies": {
        "turbo": "^1.8.6"
    },
    "scripts": {
        "dev": "turbo run dev",
        "watch": "turbo run watch",
        "build": "turbo run build",
        "start": "node apps/api/dist/main"
    },
    "workspaces": [
        "apps/*"
    ]
}
